import { ui } from "../en/ui-elements"
import { forms } from "./forms"
import { navigation } from "./navigation"
import { strings } from "./strings"
export const en = {
    strings,
    ui,
    forms,
    navigation
}
